import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AmanMap - Community-powered heatmap for safety, amenities, and livability',
  description: 'Discover safe and livable areas in Morocco through community ratings',
  keywords: 'Morocco, safety, amenities, livability, heatmap, community, ratings',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
